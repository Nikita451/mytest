<!doctype html>
<html>
	<head>
		<title></title>
	</head>
	<body>
		<h1>{{ post.getTitle()  }}</h1>
		<div>
		{{ post.getBody() }}
		</div>
		<hr>
		<p>
		{{ post.getCreatedDate().format("Y:m:d H:i:s") }}
		</p>
	</body>
</html>
